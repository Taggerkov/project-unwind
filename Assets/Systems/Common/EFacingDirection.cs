namespace Systems.Common
{
    /// <summary>The horizontal direction a combatant is facing in world space.</summary>
    public enum EFacingDirection
    {
        /// <summary>Facing the positive X axis (screen right).</summary>
        Right = 0,
        /// <summary>Facing the negative X axis (screen left).</summary>
        Left = 1
    }
}
